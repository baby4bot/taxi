# กติกาชื่อผู้ใช้ (ไอดี) — “ตรงเป๊ะ” vs “ครอบคลุม”

> เอกสารนี้ตอบคำถามเดียว: **คำต้องห้ามแต่ละคำควรตั้งโหมดไหน และโหมดนั้นบล็อกอะไรจริง ๆ**
> ทุกตัวอย่างในเอกสารนี้ **วัดจากแอปจริง** (เรียก `__usernamePolicy()` / `__reservedHit()` ในหน้าจอแอป ไม่ได้เขียนขึ้นเอง)
> รายการคำต้องห้ามแก้ได้จากในแอปโดยไม่ต้องแตะโค้ด — ดูหัวข้อ 5

---

## 1) สรุปสั้นในตารางเดียว

| | **ครอบคลุม** (ไม่มี `!`) | **ตรงเป๊ะ** (เติม `!` ท้ายคำ) |
|---|---|---|
| กติกา | ชื่อ **ขึ้นต้น** / **ลงท้าย** / **เป็นทั้งช่วง** ด้วยคำนี้ = บล็อก | ชื่อ **เท่ากับคำนี้เท่านั้น** = บล็อก |
| พฤติกรรมจริง | `admin2024` · `my-admin` · `admin.manager` · `superadmin` · `sup_port` = **โดน** | `admin` · `Admin` · `ADMIN` · `"admin "` = **โดน** · `admin2024` = **ผ่าน** |
| ใช้เมื่อ | คำที่ “ใครก็ไม่ควรแอบอ้าง” — ชื่อบทบาท/ตำแหน่ง/ระบบ (`admin` `manager` `support` `root` `police` `taxiadmin` …) | คำสั้น/คำสามัญที่คนธรรมดาเอาไปผสมชื่อได้ — `user` `help` `mod` `test` `guest` `taxi` `ceo` … |
| ราคาที่ต้องจ่าย | ถ้าตั้งคำสั้นเป็นโหมดนี้ จะ **บล็อกคน innocents** เช่น `modest88`, `helpful99`, `tester` | ถ้าตั้งคำกว้างเป็นโหมดนี้ จะ **มีช่องให้แอบอ้าง** เช่นใช้ `support01` … (แต่ยังกันด้วยคำอื่นในรายการได้) |

**ประโยคเดียวจำง่าย:** โหมด **ครอบคลุม** = “ห้ามมีคำนี้อยู่ในชื่อ” · โหมด **ตรงเป๊ะ** = “ห้ามใช้ชื่อนี้เป๊ะ ๆ แต่ไปต่อเติมได้”

---

## 2) ด่านมี 2 ชั้น — ชื่อต้องผ่านทั้งคู่

```
กรอกชื่อ
  ├─ ชั้นที่ 1 · รูปแบบ:  ^[A-Za-z0-9._-]{3,20}$     (ตัวอักษรอังกฤษ/ตัวเลข/จุด/ขีดล่าง/ขีดกลาง)
  │      ↳ 'ab' (สั้นไป) · 'ชื่อไทย' · 'good name' (มีเว้นวรรค)  →  ❌ code = format
  └─ ชั้นที่ 2 · คำต้องห้าม (หัวข้อนี้)
         ↳ ผ่านทั้งคู่ → ✅  บันทึกได้
```

ผลทดสอบชั้นที่ 1 ในแอปจริง: `ab` → `format` · `ชื่อไทย` → `format` · `good name` → `format` · `_bad` → `ok` · `Arm2539` → `ok` (ตัวพิมพ์ใหญ่เล็กใช้ได้)

**สำคัญ:** ชั้นที่ 1 มาก่อน ⇒ ตัวอักษรไทย/วรรณยุกต์/อักขระแปลก จะถูกปฏิเสธตั้งแต่ชั้นแรก **ไม่ต้องรอเทียบคำต้องห้าม**

---

## 3) ขั้นตอนเทียบคำต้องห้าม (ทำไม `@dmin` โดนด้วย)

```
1. unL33t()        → พิมพ์เล็กทั้งหมด + ถอดรหัสลวงตา
                     0→o · 1→i · 3→e · 4→a · 5→s · 7→t · 8→b · @→a · $→s · !→i · |→i
2. norm  = ตัดทุกอย่างที่ไม่ใช่ a-z0-9 ออก       ⇒  'my-admin' → 'myadmin'
3. segs  = แยกด้วยตัวคั่น (จุด/ขีด/ขีดล่าง) แล้ว unL33t ทีละท่อน
                      ⇒  'admin.manager' → ['admin','manager']
4. เทียบกับทุกคำในรายการ
     • norm === คำ            → บล็อก
     • คำนี้เป็นโหมด “ตรงเป๊ะ” → ข้ามข้อถัดไป (ไม่ดูขึ้นต้น/ลงท้าย)
     • norm ขึ้นต้น/ลงท้ายด้วยคำ → บล็อก
     • มีท่อนไหน (ที่ไม่ใช่คำเอง) ขึ้นต้น/ลงท้ายด้วยคำ → บล็อก
```

**จุดที่ต้องระวังที่สุด (เจอบั๊กจริงตอนทำ):** ต้อง **ถอดรหัสลวงตาก่อนตัดอักขระ** ถ้าตัดก่อน `@dmin` จะเหลือ `dmin` แล้ว **หลุดด่านทันที**

ผลทดสอบการถอดรหัส (โหมดครอบคลุม): `adm1n` 🚫 · `@dmin` 🚫 · `4dmin` 🚫 (ทั้งสามกลายเป็น `admin`) · `sup_port` 🚫 (ตัด `_` แล้วได้ `support` พอดี) · `my-admin` 🚫 (ตัด `-` แล้วลงท้ายด้วย `admin`) · `táxi` ผ่านการเทียบคำ **แต่ติดชั้นที่ 1 (รูปแบบ) อยู่ดี**

---

## 4) ตัวอย่างจริงจากรายการปัจจุบัน (32 คำ · 9 คำเป็นโหมดตรงเป๊ะ)

ทดลองกับชื่อ 55 ชื่อในแอปจริง: **รายการปัจจุบันบล็อก 38 ชื่อ** · ถ้าเปลี่ยน **ทุกคำ** เป็นโหมดครอบคลุมจะบล็อก **47 ชื่อ** ⇒ ต่างกัน 9 ชื่อ และ 9 ชื่อนี้คือคนธรรมดาที่โหมดตรงเป๊ะช่วยไว้:

| ชื่อที่ทดสอบ | รายการปัจจุบัน | ถ้าคำเป็น “ครอบคลุม” หมด | คำที่จับในโหมดครอบคลุม |
|---|---|---|---|
| `user2539` | ✅ ผ่าน | 🚫 โดน | `user` |
| `user1` | ✅ ผ่าน | 🚫 โดน | `user` |
| `myuser` | ✅ ผ่าน | 🚫 โดน | `user` |
| `tester` | ✅ ผ่าน | 🚫 โดน | `test` |
| `test123` | ✅ ผ่าน | 🚫 โดน | `test` |
| `guest1` | ✅ ผ่าน | 🚫 โดน | `guest` |
| `helpful99` | ✅ ผ่าน | 🚫 โดน | `help` |
| `modest88` | ✅ ผ่าน | 🚫 โดน | `mod` |
| `taxidriver` | ✅ ผ่าน | 🚫 โดน | `taxi` |

และชื่อเหล่านี้ **โดนทั้งสองโหมด** (เป็นเป้าหมายจริงของการมีด่าน): `admin` · `Admin` · `administrator` · `admin2024` · `my-admin` · `admin.manager` · `superadmin` · `sysadmin` · `taxiadmin` · `support` · `support.th` · `sup_port` · `taxisupport` · `manager99` · `root` · `system` · `moderator` · `police` · `police1` · `official` · `staff` · `employee` · `owner` · `boss` · `freebuff` · `codebuff` · `taxi` · `user` · `help` · `test` · `guest` · `ceo` · `cfo` · `cto` · `adm1n` · `@dmin` · `4dmin`

**ยังผ่านได้ทั้งสองโหมด (และผ่านได้ควรจะผ่าน):** `arm2539` · `somchai` · `nvy` · `rareitem` · `driver_bkk` · `tx2026` · `badminton` · `sudo`

> 🔎 **ช่องที่เจอจากการทดสอบ:** `sudo` ผ่านทั้งสองโหมด (ไม่มีคำ `sudo` ในรายการ — คำ `root`/`system` จับไม่ได้เพราะเทียบแบบขึ้นต้น/ลงท้าย/ท่อน) ถ้าเจ้าของแอปอยากกันด้วย ให้เพิ่ม `sudo` เป็นโหมด **ครอบคลุม**

---

## 5) อยากเห็นความต่างสด ๆ — ทดสอบเองในแอป (ไม่กินโควตา)

1. เข้า **ศูนย์เมนู → หมวดสมาชิก → “ไอดีต้องห้าม”** (ต้องมีสิทธิ์ `canManageReservedNames` — ค่าเริ่มต้น = Admin)
   · ชื่อ เมนูย่อให้อยู่บรรทัดเดียว — ความหมาย “ห้ามตั้งชื่อเลียนแบบแอดมิน” อยู่ในบรรทัดคำอธิบายของการ์ด
2. ในกล่อง **🧪 ลองพิมพ์ชื่อดูผลทันที** พิมพ์ `taxi`
3. กดสวิตช์ของคำ `taxi` ให้เป็น **โหมดครอบคลุม** → พิมพ์ `mytaxi` ⇒ ขึ้น 🚫 ทันที
4. สลับสวิตช์กลับเป็น **ตรงเป๊ะ** → `mytaxi` ⇒ กลับขึ้น ✅ (แต่ `taxi` ยัง 🚫)
5. กด **บันทึก** เมื่อพอใจ (ปุ่ม “คืนค่าเริ่มต้น” = ดึงรายการมาตรฐานกลับมา โดยยังไม่บันทึกจนกดบันทึก)

กล่องทดลองนี้ทำงานกับ **รายการที่ยังไม่บันทึก** ⇒ ลองสลับโหมดไปมาได้โดยไม่กระทบผู้ใช้จริง

---

## 6) เก็บที่ไหน · มีผลเมื่อไหร่ · ใครแก้ได้

| หัวข้อ | รายละเอียด |
|---|---|
| เอกสาร | `settings/reserved_usernames` = `{ list, selfChange, updatedAt, updatedBy }` |
| รูปแบบ `list` | **ข้อความบรรทัดละคำ** · เติม `!` ท้ายคำ = โหมดตรงเป๊ะ<br>`admin`<br>`manager`<br>`user!`<br>`taxi!` |
| มีผลเมื่อไหร่ | เครื่องที่กดบันทึก = **ทันที** (อัปเดตแคช `taxi_reserved_words_v1`) · เครื่องอื่น = ตอนเปิดแอปครั้งถัดไป |
| ใครแก้ได้ | สิทธิ์ `canManageReservedNames` (ปรับได้ใน **ตั้งค่าสิทธิ์การใช้งาน** → `perm_canManageReservedNames`) — เช็คซ้ำทั้งตอน **เปิดหน้า** และตอน **บันทึก** |
| ดึงไม่ได้ (เน็ต/โควตาหมด) | ใช้ค่าเริ่มต้นในโค้ด (`RESERVED_SEED`) — **ห้ามปล่อยให้ด่านนี้ว่าง** |
| รายการว่าง | = **ไม่มีด่านห้ามไอดีเลย** (หน้าจอเตือนไว้) ต้องมีอย่างน้อย 1 คำ |
| สถานะจริงบนเซิร์ฟเวอร์ (17 ก.ย. 69) | **สร้างเอกสารแล้ว 32 คำ** เท่ากับ `RESERVED_SEED` เป๊ะ · `selfChange = 0` (ไอดีล็อก) · `GET` = 200 (ก่อนหน้านี้ 404 ทุกครั้งที่เปิดแอป) |
| กติกาความปลอดภัย | ต้องมี `reserved_usernames` ในรายการ docId ของ `settings` ในกติกาที่วางจริง **ไม่งั้นกดบันทึกแล้วถูกปฏิเสธ** — ใส่ให้แล้วใน `firestore.rules` / `firestore-auth.rules` (ฉะบับเข้ม: เขียนได้เฉพาะ `isStaff()`) |
| บัญชีเก่า | **ไม่ถูกบังคับเปลี่ยนชื่อ** และไม่ถูกตัดสิทธิ์ — กติกานี้มีผลกับ “การตั้งชื่อใหม่” เท่านั้น |

---

## 7) กับดัก 7 ข้อ (อ่านก่อนแก้โค้ด)

1. **ถอดรหัสลวงตาก่อนตัดอักขระเสมอ** — ไม่งั้น `@dmin` หลุด (ดูหัวข้อ 3)
2. **คำสั้นต้องเป็นโหมดตรงเป๊ะ** — `user`/`help`/`mod`/`test` ถ้าตั้งครอบคลุม จะบล็อก `tester`, `modest88`, `helpful99` ฟรี ๆ (ดูตารางหัวข้อ 4)
3. **ตัวพิมพ์ใหญ่เล็กไม่มีผล** — `Admin` = `admin` = `ADMIN` ทั้งการเทียบคำและการเก็บ (`toLowerCase()`)
4. **เว้นวรรคท้ายชื่อไม่ช่วย** — `"taxi "` ถูกตัดเหลือ `taxi` ⇒ ยังโดน
5. **จุด/ขีด/ขีดล่างไม่ช่วย** — `my-admin` · `my_admin` · `my.admin` ⇒ `myadmin` (ยังโดน) และถูกแยกเป็นท่อนให้จับได้อีกชั้น
6. **ชื่อที่ระบบสร้างจากอีเมล Google ต้องเช็คด้วย** — ถ้าอีเมลคือ `admin@gmail.com` แล้วระบบตั้งชื่อ `admin` ให้อัตโนมัติ = **ต้องไม่ให้ผ่าน** (โค้ดเติม `_uid` ท้ายให้)
7. **ทุกทางที่ตั้งชื่อต้องเรียกด่านเดียวกัน** — สมัคร · เปลี่ยนชื่อเอง · แอดมินสร้าง/แก้บัญชี · ชื่อจากอีเมล Google ถ้าลืมทางใดทางหนึ่ง ด่านก็ไร้ค่า

---

## 8) สำหรับนักพัฒนา — ที่เรียกใช้ + ใช้ซ้ำในโปรเจกต์อื่น

**ฟังก์ชันในโปรเจกต์นี้**

| ฟังก์ชัน | ทำอะไร |
|---|---|
| `__validUsername(u)` | ชั้นที่ 1 — รูปแบบ `^[A-Za-z0-9._-]{3,20}$` |
| `__unL33t(s)` | ถอดรหัสลวงตา (0→o 1→i 3→e 4→a 5→s 7→t 8→b @→a $→s !→i \|→i) |
| `__reservedParse(raw?)` | แปลง `"บรรทัดละคำ (เติม !)"` ↔ `[{w,e}]` (รับได้ทั้ง string และ array) |
| `__reservedHit(name, words?)` | คืน “คำที่ชน” (`''` = ผ่าน) · `words` ใส่มาได้เพื่อทดสอบรายการที่ยังไม่บันทึก |
| `__usernamePolicy(name, words?)` | รวมทุกอย่าง → `{ ok, code: 'empty'\|'format'\|'reserved'\|'ok', word, msg }` (ข้อความไทยพร้อมโชว์) |
| `__reservedFetch()` | ดึง `settings/reserved_usernames` → เขียนแคช (พลาด = ใช้ค่าเริ่มต้น) |
| `__selfChangeLimit()` | นโยบาย “ผู้ใช้ตั้งไอดีเองได้กี่ครั้ง” (0 = ล็อกถาวร, fail-closed) |

**จุดเรียกที่ต้องครบ 4 ทาง:** `registerApp()` (สมัคร) · `saveUsernameChange()` (เปลี่ยนเอง) · `saveAdminUserEdit()` (แอดมินแก้) · `__resolveGoogleAccount()` (ชื่อจากอีเมล Google)

**โครงโค้ดใช้ซ้ำได้ (ไม่ผูกกับแอปนี้ — คัดไปวางโปรเจกต์อื่นได้เลย)**

```js
// กติกาชื่อต้องห้ามแบบ 2 โหมด — พร้อมใช้ (Vanilla JS)
const unL33t = s => String(s || '').toLowerCase()
  .replace(/[0134578@$!|]/g, c => ({ '0':'o','1':'i','3':'e','4':'a','5':'s','7':'t','8':'b','@':'a','$':'s','!':'i','|':'i' }[c]));

const parseWords = raw => String(raw || '').split('\n').map(s => s.trim()).filter(Boolean)
  .map(line => ({ w: line.replace(/!+$/, '').toLowerCase().replace(/[^a-z0-9]/g, ''), e: /!$/.test(line) }))
  .filter(x => x.w.length >= 2);

const hitWord = (name, words) => {
  const raw  = String(name || '');
  const norm = unL33t(raw).replace(/[^a-z0-9]/g, '');            // 1) ถอดรหัสก่อนตัดอักขระ
  const segs = raw.toLowerCase().split(/[^a-z0-9]+/).map(unL33t).filter(Boolean);
  for (const it of words) {
    if (norm === it.w) return it.w;                              // ตรงเป๊ะเท่านั้นก็จับได้
    if (it.e) continue;                                          // โหมดตรงเป๊ะ → ไม่ดูขึ้นต้น/ลงท้าย
    if (norm.startsWith(it.w) || norm.endsWith(it.w)) return it.w;
    if (segs.some(s => s !== it.w && (s.startsWith(it.w) || s.endsWith(it.w)))) return it.w;
  }
  return '';
};

const checkUsername = (u, rawList) => {
  const name = String(u || '').trim();
  if (!name) return { ok: false, code: 'empty' };
  if (!/^[A-Za-z0-9._-]{3,20}$/.test(name)) return { ok: false, code: 'format' };
  const hit = hitWord(name, parseWords(rawList));
  return hit ? { ok: false, code: 'reserved', word: hit } : { ok: true, code: 'ok' };
};

// ทดลองเร็ว ๆ (ผลตรงกับแอปจริง)
const list = 'admin\nmanager\nsupport\nuser!\nhelp!\nmod!\ntest!\nguest!\ntaxi!';
['admin2024','my-admin','sup_port','adm1n','user2539','modest88','tester','taxi','mytaxi','arm2539']
  .forEach(n => console.log(n.padEnd(12), checkUsername(n, list)));
```

**ใช้ใน CI / เทสต์อัตโนมัติ (ไม่ต้องมีเน็ต):** เอาโค้ดข้างบนไปวางในเทสต์ แล้ว assert ตารางในหัวข้อ 4 ได้ตรง ๆ — เป็นชุดเคสที่วัดจากของจริงแล้ว ไม่ใช่ตัวอย่างสมมติ

---

## 9) อ้างอิงในโปรเจกต์นี้

- `README.md` → หัวข้อ “ชื่อผู้ใช้ (username) — ล็อกถาวร · คำต้องห้าม” และ “หน้าแอดมิน ‘ไอดีต้องห้าม’”
- `.freebuff/run.md` → “ชื่อต้องห้าม / เลียนแบบบทบาท — กติกาที่ห้ามพลาด” และ “หน้าแอดมินแก้รายการไอดีต้องห้าม (draft + ช่องลองพิมพ์)”
- หน้าจอจริงในแอป: **ศูนย์เมนู → สมาชิก → ไอดีต้องห้าม** (หัวข้อในหน้าต่างก็ใช้ชื่อเดียวกัน)
