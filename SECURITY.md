# 🔐 ความปลอดภัยของฐานข้อมูล (Firestore)

> เอกสารนี้เขียนไว้ให้ **คนอื่นเอาไปใช้ได้เลย** — ไม่ใช่เฉพาะแอปแท็กซี่นี้
> ทุกอย่างที่อ้างถึงมีไฟล์จริงในเรโป: [`firestore.rules`](firestore.rules) และ [`tests/security-check.ps1`](tests/security-check.ps1)

---

## 1. สถานะปัจจุบัน (ตรวจแล้ว 12 ก.ย. 2569)

รันสคริปต์ตรวจโดย **ไม่ใช้การล็อกอินเลย** (เหมือนคนนอกยิงเข้ามา) ได้ผล **7/7 ข้อเปิดอยู่**:

| ตรวจ | HTTP | ผล | ความหมาย |
|---|---|---|---|
| อ่านเอกสารสมาชิกตาม id | 200 | 🔴 เปิด | อ่าน **รหัสผ่าน (แฮช) + เบอร์โทร** ของสมาชิกได้ |
| อ่าน `settings/changelog` | 200 | 🔴 เปิด | เอกสารตั้งค่าทั้งหมดเปิดอ่าน |
| คิวรี `users` | 200 | 🔴 เปิด | เส้นทางที่หน้าเข้าสู่ระบบใช้ ทำงานได้โดยไม่มีสิทธิ์ |
| เขียนเที่ยวปลอมลง `users/*/trips` | 200 | 🔴 เปิด | ปลอมประวัติเที่ยว/รายได้ได้ |
| ลบเที่ยว | 200 | 🔴 เปิด | ลบประวัติรายได้ทิ้งได้ |
| เขียนคอลเลกชันมั่ว `_secprobe` | 200 | 🔴 เปิด | ใช้โปรเจกต์เป็นที่ฝากไฟล์/ฐานข้อมูลส่วนตัวได้ |
| ลบรายการ `global_logs` | 200 | 🔴 เปิด | **ลบร่องรอยการใช้งานทิ้งได้** |

รันเองได้ทุกเมื่อ:

```powershell
powershell -ExecutionPolicy Bypass -File tests/security-check.ps1
# โปรเจกต์ของคุณเอง:
powershell -ExecutionPolicy Bypass -File tests/security-check.ps1 -ProjectId my-project -ApiKey AIza...
```

สคริปต์นี้ **ไม่แตะข้อมูลจริง** — เขียน/ลบเฉพาะเส้นทางทิ้ง `users/__secprobe/trips/__probe` แล้วเก็บกวาดทิ้งทันที (รหัสออก 0 = ปลอดภัย, 1 = ยังมีช่องเปิด)

---

## 2. ทำไมมันเป็นแบบนี้ (และทำไมปิดไม่หมดในวันเดียว)

แอปนี้ล็อกอินด้วย **Username + รหัสผ่านของตัวเอง** เก็บไว้ใน Firestore — ไม่ได้ใช้ **Firebase Authentication**
ผลคือต่อให้เขียนกติกาแบบ `if request.auth != null` ก็ไม่ช่วยอะไร เพราะ **ไม่มีคำขอไหนมีตัวตนเลย** (รวมถึงแอป我们自己)

```
กติกาที่ใช้ไม่ได้ตอนนี้:  request.auth != null        → ไม่มี auth → false เสมอ → แอปก็โดนบล็อกไปด้วย
กติกาที่ใช้ได้ตอนนี้:     ตรวจ "รูปแบบข้อมูล" (เขียนอะไรได้/ไม่ได้)  ← คือสิ่งที่ไฟล์ firestore.rules ทำ
กติกาที่ปิดประตูจริง:     App Check (ต้องมี token จากแอป) + Firebase Auth (ต้องรู้ว่าเป็นบัญชีใคร)
```

> 📌 API key ของ Firebase ที่ฝังอยู่ในหน้าเว็บ **ไม่ใช่ความลับ** — มันระบุ "โปรเจกต์" ไม่ได้ให้สิทธิ์อะไร
> ความปลอดภัยของ Firestore มาจาก **กติกา (Rules)** และ **App Check** เท่านั้น

---

## 3. ขั้นที่ 1 — วางกติกาชุดใหม่ (5 นาที, ทำได้เลย)

**วิธีวางผ่านหน้าเว็บ Firebase Console**
1. เปิด [Firebase Console](https://console.firebase.google.com/) → โปรเจกต์ของคุณ
2. เมนูซ้าย → **Firestore Database** → แท็บ **Rules**
3. ลบของเดิมทั้งหมด → วางเนื้อหาไฟล์ [`firestore.rules`](firestore.rules) → กด **Publish**
4. รัน `tests/security-check.ps1` ซ้ำ

**วิธีผ่าน CLI** (ถ้ามี Node + Firebase CLI)

```bash
firebase deploy --only firestore:rules
```

### หลังวางแล้วจะปิดอะไรได้ทันที

| ตรวจ | ก่อน | หลัง | เหตุผล |
|---|---|---|---|
| เขียนเที่ยวปลอม (รูปแบบมั่ว) | 200 | **403** | กติกาบังคับชื่อฟิลด์/ช่วงค่าของเที่ยว |
| ลบเที่ยว | 200 | **403** | เที่ยวเป็น append-only (แอปไม่เคยลบ — ห้ามลบจากภายนอก) |
| เขียนคอลเลกชันมั่ว | 200 | **403** | มี catch-all `if false` ปิดทุกคอลเลกชันที่ไม่รู้จัก |
| ลบ `global_logs` / `users/*/logs` | 200 | **403** | บันทึกการใช้งานเป็น append-only (ลบร่องรอยไม่ได้) |
| ลบเอกสาร `settings/*` | 200 | **403** | กันตั้งค่าหายทั้งชุด |

### ⚠️ สิ่งที่ยังเปิดอยู่โดยตั้งใจ (และจะปิดได้ด้วยขั้นที่ 2)

- **อ่าน** ข้อมูลทั้งหมดได้ (ต้องเปิด เพราะหน้าเข้าสู่ระบบต้องค้นหา username) → เห็นแฮชรหัสผ่าน/เบอร์โทร
- **เขียนข้อมูลที่ "รูปแบบถูกต้อง"** ได้ เช่น สร้างเที่ยวปลอมที่มีฟิลด์ครบ → กติกาตรวจ "หน้าตาข้อมูล" ไม่ได้ตรวจ "你是谁"
- **ลบเอกสารสมาชิก** ได้ (แอดมินใช้จริง ลบไม่ได้ก็เสียฟีเจอร์)

> ถ้าแอปเพิ่มฟิลด์ใหม่ใน `trips` หรือ `logs` **ต้องอัปเดตกติกาใน firestore.rules คู่กันทุกครั้ง**
> ไม่งั้นการบันทึกจะถูกปฏิเสธ (เป็นข้อดี: บังคับให้กติกากับแอปตรงกันเสมอ — ทดสอบก่อน deploy)

---

## 4. ขั้นที่ 2 — เปิด App Check (ปิดประตูจริง)

App Check ทำให้ Firestore ยอมรับเฉพาะคำขอที่ **มาจากแอปจริง** (มี token ที่ออกโดย reCAPTCHA) — สคริปต์หรือคนนอกที่รู้ API key จะโดนปฏิเสธทั้งหมด

**โค้ดเตรียมไว้แล้วใน `index.html`** — ไม่ต้องเขียนเพิ่ม:

```js
const APP_CHECK_SITE_KEY = ""; // ← วาง site key ที่นี่ (เว้นว่าง = ปิด ไม่กระทบพฤติกรรมเดิม)
```
```js
window.__fsHeaders()  // แนบ token ให้ทุกคำขอ REST สำรองทั้ง 7 จุดโดยอัตโนมัติ
```

### 🧰 เครื่องมือตรวจในแอป (ไม่ต้องเดา)

โค้ดเตรียมไว้ให้แล้ว— เปิดแอปแล้วเรียกในคอนโซลของเบราว์เซอร์ (F12):

```js
await appCheckStatus()     // configured / ready / token (ไม่ยิงเครือข่าย)
await appCheckSelfTest()   // 📊 ยิงคำขอจริง 2 ครั้ง: ไม่มีโทเคน vs มีโทเคน → บอกชัดว่าต้องทำอะไรต่อ
```

`appCheckSelfTest()` ตอบเป็นตาราง 4 กรณี (คือเข็มทิศของขั้นตอนทั้งหมดนี้):

| withoutToken | withToken | ความหมาย | ต้องทำอะไร |
|---|---|---|---|
| 200 | 200 | ยังไม่บังคับใช้ | ดู Metrics ให้ verified ครบก่อน → ค่อย Enforce |
| **403** | **200** | ✅ **สมบูรณ์** | Enforce ทำงานแล้ว + แอปส่งโทเคนผ่านครบ |
| 403 | 403 | ⛔ **อันตราย** | แอปจริงส่งโทเคนไม่ผ่าน → กด **Unenforced** ก่อน แล้วตรวจ site key/โดเมน |
| 0 | — | ขอโทเคนไม่ได้ | ตรวจ site key · โดเมนใน reCAPTCHA · ลงทะเบียนแอปในคอนโซลแล้วหรือยัง |

### 🔢 ลำดับที่ต้องทำ (สลับลำดับ = แอปอ่าน/เขียนไม่ได้เลย ⚠️)

1. ขอ **site key และ secret key** ที่ [reCAPTCHA admin](https://www.google.com/recaptcha/admin/create)
   → กรอกหน้าลงทะเบียน:

   | ช่อง | กรอกว่า |
   |---|---|
   | ป้ายกำกับ | `taxi-meter` (ตั้งชื่ออะไรก็ได้ — ใช้หาง่าย ๆ ตอนกลับมาแก้) |
   | ประเภท reCAPTCHA | **อัจฉริยะแบบคะแนน (v3)** — ⚠️ ห้ามเลือก v2 (จะให้คนขับแก้ภาพบิดเบี้ยว) |
   | โดเมน | `baby4bot.github.io` แล้วกด Enter · เพิ่มอีกอัน: `127.0.0.1` (สำหรับทดสอบในเครื่อง) |
   | GOOGLE CLOUD PLATFORM → ชื่อโปรเจกต์ | เลือกโปรเจกต์ **เดียวกับ Firebase** — ดูชื่อที่ Firebase Console → ⚙️ Project settings → General (Project ID = `mytalkie-3955a`) แล้วไปหาในลิสต์ · ห้ามเลือก `Default Gemini Project`<br>**ถ้าหาไม่เจอ/ชื่อซ้ำจนแยกไม่ออก:** เลือกโปรเจกต์ขององค์กรตัวเองอันไหนก็ได้ — ไม่มีผลต่อการทำงาน เพราะโควตาฟรีก็นับรวมทั้งองค์กร และตัวที่ทำให้ App Check ทำงานจริงคือ **ตอนวาง secret key ที่ Firebase Console → App Check → Apps** · ใช้ `appCheckSelfTest()` เป็นตัวตัดสินสุดท้าย |

   กด **ส่ง** → หน้าใหม่จะแสดง **Site key** และ **Secret key** (คัดลอกไว้ทั้งคู่)
2. **Firebase Console → App Check → Apps** → เลือกเว็บแอป → ติ๊ก **reCAPTCHA v3**
   → วาง **secret key** (คีย์ลับ ไม่ใช่ site key) → Save
   — ข้อนี้คนลืมบ่อยที่สุด: ต้องใช้ **2 คีย์ต่างกัน** (site key → โค้ด, secret key → คอนโซล)
3. วาง **site key** ที่ `APP_CHECK_SITE_KEY` ใน `index.html` (บรรทัดเดียว) แล้วอัปโหลดขึ้นโฮสต์
4. เปิดแอป → F12 → `await appCheckSelfTest()` → ต้องเห็น `withToken 200`
   • ทดสอบในเครื่อง (`127.0.0.1`): SDK จะพิมพ์ **debug token** ในคอนโซล → เอาไปลงที่
     App Check → Apps → จัดการ → **Debug tokens** (โทเคนนี้เฉพาะเครื่องนั้น ห้ามแจก)
5. **ยังไม่ต้อง Enforce** — กลับไปดู **App Check → Metrics** 1–2 วัน ว่าคำขอขึ้น "Verified" เกือบ 100%
6. ค่อยกด **App Check → APIs → Cloud Firestore → Enforce** → กลับมาเรียก `appCheckSelfTest()` ซ้ำ → ต้องขึ้น **403 + 200**

### ⚙️ ตั้งค่าที่ควรรู้ (ไม่ตั้งก็ได้)

| ค่า | ค่าเริ่มต้น | แนะนำ |
|---|---|---|
| App Check token TTL | 1 วัน | ปล่อยไว้ (สั้นเกิน = ขอถี่ เปลืองโควตา) |
| reCAPTCHA risk threshold | 0.5 | ปล่อยไว้ · อย่าตั้ง 1.0 (คนจริงจะโดนบล็อกด้วย) |
| โควตา reCAPTCHA | — | ฟรี **10,000 ครั้ง/เดือน** (นับรวมทั้งองค์กร — ลดจากเดิม 1 ล้าน เมื่อ Google ย้ายมาใช้ Cloud Fraud Defense) |

### 🧮 โควตาพอไหม? (คำนวณให้แล้ว)

App Check ขอโทเคนใหม่ **เมื่อโทเคนเก่าหมดอายุครึ่งทาง** (TTL เริ่มต้น 1 วัน → ขอประมาณ 2–3 ครั้ง/เครื่อง/วัน)

| จำนวนเครื่อง | ใช้ต่อเดือน (ประมาณ) | เทียบโควตาฟรี 10,000 |
|---|---|---|
| 5 | ~450 | เหลือเฟือ |
| 30 | ~2,700 | เหลือเฟือ |
| 100 | ~9,000 | เริ่มตึง → เพิ่ม TTL |

> ถ้าเริ่มใกล้เต็ม: Firebase Console → App Check → Apps → ตั้ง **token TTL** เป็น 3–7 วัน
> (ยิ่ง TTL ยาว = ขอประเมินถี่น้อยลง = ประหยัดโควตา แต่ถ้าโทเคนหลุดจะถูกใช้ซ้ำได้นานขึ้น)

### หลัง Enforce แล้วจะได้

| ตรวจ | ผลที่ควรได้ |
|---|---|
| `tests/security-check.ps1` | **LOCKED ทั้ง 7 ข้อ** |
| ไม่มีโทเคน → อ่าน `settings/changelog` / คิวรี users / เขียนเที่ยว | **403** |
| ตัวแอปเอง | ทำงานปกติ (เพราะแนบโทเคนอัตโนมัติทุกคำขอ) |

> ถ้าแอป "ค้าง/โหลดไม่ได้" หลัง Enforce → กด **Unenforced** กลับก่อน (มีผลทันที) แล้วตรวจ site key/โดเมน/
> debug token ก่อนลองใหม่ — ***อย่าลืมว่ายังไม่เคยทดสอบการขอโทเคนจากแอปจริงในโหมด Monitor มาก่อน***

### 😛 สิ่งที่ App Check ไม่ได้ปิด

คนที่เปิดแอปจริงในเบราว์เซอร์จะได้โทเคนไปด้วย → ยังใช้ DevTools ยิงคำขอที่กติกาอนุญาตได้
App Check จึงเป็น "กันบอต/กันคนไม่มีแอป" ไม่ใช่ "รู้ว่าเป็นบัญชีใคร" — ต้องอ่านหัวข้อ 5 ต่อ

---

## 5. ขั้นที่ 3 — Firebase Auth (ทางที่แข็งที่สุด, งานใหญ่กว่า)

App Check พิสูจน์ว่า "คำขอมาจากแอปจริง" แต่ **ไม่รู้ว่าเป็นบัญชีใคร** — ดังนั้นด่าน "ยืนยันตัวตนซ้ำก่อนแก้ข้อมูลสำคัญ 7 จุด" ที่มีในแอป ยังถูกข้ามได้ถ้ามีคนยิง API ตรง ๆ

👉 **คู่มือเต็มอยู่ที่ [`FIREBASE-AUTH.md`](FIREBASE-AUTH.md)** — สอนทีละขั้นพร้อมโค้ดจริงของแอปนี้ (3 แนวทาง + ตารางเลือก + ทดสอบ + ย้อนกลับ + ตาราง error)

| แนวทาง | ปิดช่องได้ | ต้องมี | งานฝั่งแอป |
|---|---|---|---|
| A. App Check | กันสคริปต์/บอต (ไม่กันคนเปิดแอปจริง) | โดเมน | แทบไม่มี |
| B. Auth + อีเมลจริง | ~95% | อีเมลคนขับทุกคน | ปานกลาง |
| **C. Auth + Custom Token ผ่าน Cloud Functions** ⭐ | **ปิดจริง 100%** | เปิด Blaze (มีโควตาฟรี) | ปานกลาง |

แนวทาง C คือสิ่งที่แนะนำ เพราะแอปนี้ล็อกอินด้วย username/รหัสผ่านของตัวเอง (ไม่ใช้อีเมล):

1. ล็อกอิน → เรียก **Cloud Function** ให้ตรวจรหัสผ่านฝั่งเซิร์ฟเวอร์ (แฮชไม่ต้องส่งออกจากฐานข้อมูลอีก)
2. เซิร์ฟเวอร์ออก **custom token** ที่ตั้ง `uid` = docId ของผู้ใช้ → client `signInWithCustomToken()`
3. วางกติกา [`firestore-auth.rules`](firestore-auth.rules) → ตรวจด้วยการอ่าน/เขียนเอกสารของตัวเองเท่านั้น
4. ยืนยันด้วย `tests/security-check.ps1` → ต้องขึ้น **LOCKED ทั้ง 7 ข้อ**

> ⚠️ แนวทาง B (อีเมลจริง) แม้ฟรี แต่จะทำให้ "ลืมรหัสผ่านแบบยืนยัน username + เบอร์โทร" ที่ใช้อยู่หายไป
> และแอดมินจะตั้งรหัสผ่านให้คนอื่นไม่ได้ → เหมาะกับแอปใหม่มากกว่า

---

## 6. ข้อมูลอ่อนไหวที่ต้องรู้ว่ามีอยู่

| ที่เก็บ | มีอะไร | ความเสี่ยงถ้าหลุด |
|---|---|---|
| `users/{uid}` | `password` (แฮช), `phoneNumber`, `sessionId`, `role`, `extraPerms` | แฮช + เบอร์โทร → ใช้โจมตี/หลอกลวงได้ (แฮชยังต้องถอดรหัสก่อน แต่ไม่ควรเปิดให้โหลด) |
| `users/{uid}/trips/*` | ประวัติเที่ยว รายได้ จุดหมาย | ข้อมูลส่วนตัวของคนขับและผู้โดยสาร |
| `global_logs`, `users/{uid}/logs` | ร่องรอยกิจกรรม | ลบแล้วตรวจสอบย้อนหลังไม่ได้ |
| `settings/*` | กติกาสิทธิ์ ข้อความแจ้งเตือน ประวัติเวอร์ชัน | แก้แล้วมีผลทั้งระบบ |

---

## 7. เช็กลิสต์ก่อน/หลังแก้

- [ ] **สำรองข้อมูลก่อน** (Firestore → Export) — กติกาผิดพลาดจะทำให้แอปเขียนไม่ได้ทันที
- [ ] วาง `firestore.rules` → Publish
- [ ] เปิดแอปจริงทดสอบให้ครบ: ล็อกอิน · เริ่ม/จบเที่ยว · บันทึกโปรไฟล์ · เปลี่ยนธีม · เปิดประวัติเที่ยว · แอดมินแก้/ระงับสมาชิก · Service Portal · ประวัติเวอร์ชัน
- [ ] รัน `tests/security-check.ps1` ซ้ำ (คาดหวัง: 4 ข้อที่ควรปิด กลายเป็น LOCKED)
- [ ] ถ้าทำขั้นที่ 2: ยืนยัน Metrics เป็น Verified → Enforce → รันสคริปต์ซ้ำอีกครั้ง
- [ ] อัปเดตกติกาในเรโปทุกครั้งที่แอปเพิ่มฟิลด์ใหม่

## 8. กฎที่ห้ามละเมิด

1. **ห้าม commit service account key / ไฟล์ `.json` ของ Firebase** ลงเรโป (เป็นกุญแจเข้าทั้งโปรเจกต์ ไม่ใช่ API key)
2. **ห้ามเปิด Enforce ของ App Check ก่อนตรวจ Metrics** — แอปจะล็อกตัวเองออกจากฐานข้อมูลทันที
3. **ห้ามลบ catch-all `match /{document=**} { allow read, write: if false; }`** ออก — เป็นตาข่ายชั้นสุดท้ายที่กันคนสร้างคอลเลกชันใหม่
4. **ห้ามตรวจสิทธิ์ในกติกาด้วยชื่อตำแหน่งที่ผู้ใช้แก้เองได้** (เช่น admin สร้าง role ใหม่) — ใช้สิ่งที่แยกแยะได้จริงเท่านั้น (ตอนนี้คือ "ตรวจไม่ได้" → ต้องรอขั้นที่ 3)
